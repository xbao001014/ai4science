# Pathology AI Knowledge Graph — core + Gap UI + full-text fetch
# Python >=3.10,<3.13 recommended
#
#   pip install -r requirements.txt
# Dev / tests / PPTX export:  pip install -r requirements-dev.txt

# Core
biopython>=1.83
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.66.0
pandas>=2.2.0
numpy>=2.0.0             # local Method-family ridge classifier
openpyxl>=3.1.0          # Excel IF import (pandas.read_excel)
pydantic>=2.7.0
rapidfuzz>=3.9.0         # journal name normalization

# LLM / extraction
openai>=1.30.0

# Graph
networkx>=3.3
pyvis>=0.3.2             # KG HTML export (visualize)

# Gap UI
streamlit>=1.55.0
plotly>=5.22.0

# Full-text PDF / MinerU (fetch-fulltext tier 2; may pull torch)
scansci-pdf>=0.1.0
mineru[core]>=2.0.0
